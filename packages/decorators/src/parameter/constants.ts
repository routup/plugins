export enum ParameterType {
    CONTEXT = 'context',
    RESPONSE = 'response',
    REQUEST = 'request',
    NEXT = 'next',
    PARAM = 'param',
    HEADER = 'header',

    COOKIE = 'cookie',
    QUERY = 'query',
    BODY = 'body',
}
